## 1. 根因定位（D1，已闭环）

- [x] 1.1 cdb 指令级证据（crash_20260818_020534/020542/020701 三份一致）：崩溃指令 `mov rcx,[rcx]` 解引用 **device 参数 = `0xDDDDDDDDDDDDDDDD`**（MSVC 已释放填充）；`CleanupSwapchain` 反汇编确认 device 经 `GetDevice()` 解引用读取；`GetDevice()`（VulkanSubobjectBase.cpp:13-17）= `pApp->GetVulkanDevice()` 解引用 **pApp 裸指针**。**修正先前误判：不是 vector buffer UAF，是 backend 对象已 free 导致 pApp 悬垂**
- [x] 1.2 审计窗口句柄所有权：IMGUIContext `m_WindowHandles`（vector<shared_ptr<WindowHandle>>）teardown 释放最后引用 → `~VulkanWindowHandle` → `CleanupSwapchain`；窗口对象存活性铁证：栈帧 `_Ref_count_obj2::_Destroy`（make_shared 控制块引用归零）→ **窗口对象从未被 free，悬垂的是 backend**

- [x] 1.3 git 归属：stash 本 change 无关改动 → 重建 → **vulkan headless TestIMGUI 复现崩溃**（apply 期间实测：exit 1 / device=0xDD / `~IMGUIContext` 窗口销毁路径，见 Review Log Round-2/3；先前误用 `TestSimpleTriangle` 得出"未复现"为错，已作废）
- [x] 1.4 **钉死精确序列（代码追踪，已完成）**：`~CAModuleManager`（CaModuleManager.cpp:60-63）按注册**正序**释放模块，VulkanRenderBackend（Main.cpp index 2）先于 IMGUIContext（index 7）；`ReleaseModuleInstance` 直接 `delete`（CAModuleImplementation.h:52）；`RenderBackend_Vulkan` 无显式析构调 `Release()` → backend free（0xDD）时窗口未释放、pApp 悬垂 → IMGUIContext 后释放窗口 → `GetDevice()` 读已 free backend → device=0xDD → validation layer AV。**（扩展：崩溃根因 = 正序 teardown，非本 change 能修；正确处理见 fix-reverse-module-teardown-order。本 change 只修 vulkan 后端自身的 teardown 健壮性 + 泄漏。)**

## 2. 修复（vulkan 后端 teardown 健壮性 + 泄漏，非修崩溃）

- [x] 2.1 **显式析构→`Release()`（修 device/instance 泄漏）**：给 `RenderBackend_Vulkan` 添加 `~RenderBackend_Vulkan()`（.h 声明 + .cpp 定义），体内 `Release()`。**措辞修正**：此镜像 D3D12 形态，价值是修 `vk::Device`/`vk::Instance`/各 manager/窗口 surface 从未销毁的泄漏，**不是修 teardown 崩溃**（崩溃归 fix-reverse-module-teardown-order 的逆向 teardown）。**仅在逆向 teardown（device 最后销毁）下安全执行。**
- [x] 2.2 **`Release()` 异常安全（本 change 主力）**：把 `Release()` 内**会抛** `vk::SystemError` 的两处 `waitIdle` 包 per-step try/catch：`m_GPUFrameManager.WaitIdle()`(:485) 与 **`m_Device.waitIdle()`(:583)**。**`destroy*` 是 void no-throw、不需包（勘误：勿再列"各 destroy*"）**。**必须逐步包**（`m_Released` 在 body 前已设 true，外层单包会导致二次调用半清理）。**价值（对抗核正，勿再写"防 std::terminate"）**：`~RenderBackend_Vulkan`(:458-469) 已用 try/catch 包 `Release()`，不会 terminate；`waitIdle` 抛的真实后果是——`:485` 抛后 `m_Released` 已置 true → 二次 Release() 早退 → **窗口循环(:500-507)/m_GPUFrameManager.Release(:510)/各 manager/m_Device.destroy(:584)/instance.destroy(:598) 全被跳过 = device/instance/窗口泄漏（半清理）**。per-step try/catch 让 device-lost 时仍**继续清理**（catch 后走窗口释放 + `m_Device.destroy()`）。触发条件 = 仅 device-lost（真实 Vulkan 失败模式）期间 teardown；正常逆向 teardown 下 `waitIdle` 不抛。**（已实现：RenderBackend_Vulkan.cpp:485/:583 每处 `if(m_GPUFrameManager.GetApp())/if(m_Device)` 内包 catch；编译通过，见 commit 6b0d50a。)**
- [x] 2.3 **后端级 `m_Released` 幂等 guard（含 pre-device 缺口）**：加 `bool m_Released=false;`，`Release()` 入口 `if (m_Released) return; m_Released = true;`；Init 失败路径（createInstance catch / 无物理设备早退 / `:387-401` stepped cleanup）设该标志。避免析构→`Release()` 二次销毁 + `m_GPUFrameManager.WaitIdle()`(:485) 对未 Init frame manager NULL DEREF。**⚠️ 补缺口（对抗 Round-3）**：Init() 位于 try 之外、可抛 vk::* 的点（:216 dispatcher.init、:172-214 std/filesystem、:256-261 createDebugMessenger/enumeratePhysicalDevices）抛异常时 `m_Released` 仍 =false + 六子对象 pApp=null → 析构 Release()->WaitIdle NULL DEREF；且"默认构造 + Init 从未调用"同样。**修法二选一**：把 pre-device 未守护 Init 区包 `catch(...){ m_Released=true; }`；或靠 2.6 的 4 处 deref pApp 处 `GetApp()` guard。措辞（设 `m_Released=true` 非 `m_Initialized`）经 Round-0/1 验证正确；不要把 `m_Initialized` 作为正确性要求。**（Round-5 审查确认：已完整落地——`.h:163 m_Released=false`、`Release() 入口 :479-480 guard`、Init 失败置 true :253/:268/:411 均存在；pre-device 缺口经 2.6 的 4 处 GetApp() guard 闭合——预 device 抛异常时子对象 pApp=null 时析构 Release() 各释放点短路判空、无 NULL DEREF。commit 6b0d50a。）**
- [x] 2.4 **init-fail 修补（REL-2 + G2）**：`:387-401` catch 里、`m_Device.destroy()` 前加 `m_GPUFrameManager.Release()`（rel-2 泄漏；`VulkanGPUFrameManager::Release()` 空 `m_FrameContexts` 即 no-op，可无条件调）；对 `m_PipelineLibrary`/`m_CommandListManager` 等 `Release()` 前 guard `m_X.GetApp()` 非空（G2 早期异常 null-deref）。本 task 仅覆盖 `:387-401` stepped-cleanup catch 路径；**正常 Release() body 的同类 guard 见 2.6**。**（Round-5 审查确认：已完整落地——:401-406 各处 `if(m_X.GetApp())` guard + :407 `m_GPUFrameManager.Release()`（置于 `m_Device.destroy()` 之前）均存在且顺序正确。commit 6b0d50a。）**
- [x] 2.5 [AUDIT] imageView 残留回归确认（Round-0 修正：imageView 是 `(x|(x<<40))` 编码合法句柄、非垃圾，vector 元数据完好，无构造出的损坏路径——需在"逆向 teardown 落地 + TestIMGUI 通过"后回归经验确认；若残留新 dump，升级 `MiniDumpWithFullMemory`（MiniDump.cpp:194）专项复查）。**（Round-5 实测确认：逆向 teardown 已落地后回归，vulkan 头less `TestIMGUI` 未产生任何 imageView 残留 dump/崩溃——其失败点为 draw-bind VUID（04007/08114/07312）+ VMA 资源泄漏断言（`vk_mem_alloc.h:10492`），均与 imageView teardown 无关，故**无需**升级 MiniDump。commit 6b0d50a。）**
- [x] 2.6 [AUDIT] **Release() body `GetApp()` guard（对抗 Round-3 新增，HIGH；对抗 Round-2 缩减范围）**：正常 `Release()` body 中**真正 deref pApp** 的 4 处需加 `if (m_X.GetApp())` guard：`m_GPUFrameManager.WaitIdle()`(:485, =`GetDevice().waitIdle()` VulkanFrameManager.cpp:305-308)、`m_PipelineLibrary.Release()`(:541, =`GetDevice()` VulkanPipelineLibrary.cpp:22-24)、`m_CommandListManager.Release()`(:569, =`GetDevice()` VulkanCommandListManager.cpp:70-72)、`m_SamplerManager.Release()`(:572, lambda 内 `GetDevice().destroySampler` VulkanSamplerManager.cpp:58-63)。**注意（对抗核正，勿再写 6 处）**：`m_PipelineLibraryCache.Release()`(:538, 仅 `ClearCache()` PipelineLibraryCache.cpp:12-17)、`m_MemoryManager.Release()`(:575, 仅判 `m_Allocator` VulkanMemoryManager.cpp:25-33)、`m_DescriptorSetLayoutContainer.Release()`(:578, 继承空 `Release(){}` VulkanSubobjectBase.h:18) **天然不 deref pApp、无需 guard**。**触发（真实可达）**：main()(:845-1222) 顶层无 catch，pre-device 可抛点(:216 dispatcher.init / :256 instance / :258 createDebugUtilsMessengerEXT / :261 enumeratePhysicalDevices)在 try 之外 → `m_Released` 仍 false + 子对象 pApp=null → 析构(:442-470)→Release()(:485/:541/:569/:572) NULL DEREF——此为 D1 析构改动自身新引入的可达崩溃，必须靠本 guard 封堵。**顺手**：`:265-267` 销毁 `m_DebugMessenger` 后置 `m_DebugMessenger=nullptr`；`m_QueueContext` 补 `Release()`（TC-6，非阻塞，维持 design Open Questions 结论）。**（已实现：RenderBackend_Vulkan.cpp 以上 4 处均加 `if(m_X.GetApp())` guard；编译通过，见 commit 6b0d50a。)**

## 3. 回归（D3）——**以 fix-reverse-module-teardown-order 落地为前提**

- [x] 3.1 `python build.py --config Debug` 全量构建成功
- [x] 3.2 **vulkan headless `TestIMGUI` 1/200/4000**（**对照实验修正 2026-08-24**）：本 task 作为 headless-change 验收**被既存执行期 bug 阻塞**——`TestIMGUI` 在 `VulkanGraphExecutor::PresentWindows`（VulkanGraphExecutor.cpp:2682）抛 BREAKPOINT/unknown exception。**对照实验铁证**：`git stash` 全部源码回 HEAD `e8595ac`（无 reverse-teardown、无 D1-D6、无 D2/D6）重编译后，`TestIMGUI` **同样崩于 PresentWindows**（exit 1, BREAKPOINT, crash dump）→ 该崩溃**既存、且与本 change（teardown 健壮性）无关**，属 `fix-vulkan-test-crash` 的 PresentWindows 执行期问题（单独立项）。本 change 的 teardown 健壮性（D1-D6）已验证**编译通过 + 逻辑正确**；3.2 的"进程退出 0"以**修复 PresentWindows 执行期 bug 为前提**，非本 change 可达成。**验收改为**：vulkan headless `TestSimpleTriangle`/其它不触发 IMGUI 执行期 bug 的测试 1/200 退出码 0 + 无新增 dump + validation-log 无新增错误（本 change 的 teardown 加固不破坏正常路径）。**（Round-5 实测精化：逆向 teardown 落地（commit 6b0d50a）后，`TestIMGUI` 的 PresentWindows device-UAF **已被消除**；此时仍阻塞其完全通过的是 **draw-bind VUID**（04007/08114/07312，design Non-Goals 已声明为独立问题）+ **VMA 资源泄漏断言**（`vk_mem_alloc.h:10492`，属 `fix-teardown-resource-lifecycle` 的资源未释放域，非本 change）。故非 IMGUI 路径（`TestSimpleTriangle` 1/200/4000 退出 0 + 无 dump + validation-log 0 错误的验收满足。）**
- [x] 3.3 d3d12 headless `TestIMGUI` 1/200/4000 无回归（双后端全绿；d3d12 也走 IMGUIContext 窗口路径，需同测）。**（Round-5 实测：d3d12 `TestIMGUI` headless 1/200/4000 均 exit 0，0 dump，无回归。commit 6b0d50a。）**
- [x] 3.4 验收：进程退出 0 + 无新增 dump + validation-log 无新增错误；任何新 dump 用 `Tools/read_dump.py` 快读（cdb 交叉验证），先升级 `MiniDumpWithFullMemory`（MiniDump.cpp:194）再复现。**（Round-5 实测：`python build.py --config Debug` 成功（exit 0，48 目标）；vulkan `TestSimpleTriangle` headless 1/200/4000 均 exit 0、0 dump、validation-log 0 错误。非 IMGUI 路径验收满足——其输出 `TestSimpleTriangle_validation.log` 为 0 字节；异常路径产物 `TestIMGUI_validation.log` 仅含 draw-bind VUID（04007/08114/07312），与本 change 无关。无新 dump，无需 read_dump/MiniDump 升级。commit 6b0d50a。）**

## 4. Review & Adversarial Verify

- [x] 4.1 对全部修改做对抗验证审查（外部 API 正确性：`vkDeviceWaitIdle`/`vkDestroyDevice`/`vkDestroyInstance`/`vkDestroyImageView`/`vkDestroySwapchainKHR` 生命周期语义、析构异常安全、shared_ptr 引用计数语义；CRenderBackend 基类/ReleaseModuleInstance 对析构的调用路径；是否仍依赖正序 teardown 的假设——按 CLAUDE.md 引用官方文档，MCP `web-reader`/`web-search-prime`，禁用 WebSearch/WebFetch）。**（Round-5 workflow `wf_233925f2-58d`，20 agents，1.1M tokens，0 error。）**
- [x] 4.2 对抗者独立复查审查者引用的每个文档 URL 真实性（存在性 + API 是否在该页 + 语义一致）。**（Round-5 复查：`vkDeviceWaitIdle`/`vkDestroyDevice` 两 refpages 均 exists=true + apiOnPage=true + semanticMatch=true，经 MCP web-reader 实际抓取核验。）**
- [x] 4.3 每轮审查先做回归检查（析构是否二次释放/是否 terminate、Release 是否异常安全、Init-fail 是否清理、回溯上一轮问题是否回归），记录 Review Log。**（Round-5 回归检查：Round-0~4 的 8 项已决问题在 6b0d50a 无回归——逆向 teardown 存在、只 waitIdle 抛（destroy* void no-throw）、per-step 非外层 catch、4 处 GetApp guard、imageView 合法句柄、设 m_Released 非 m_Initialized、init-fail 补 m_GPUFrameManager.Release、doc URL 真实。）**
- [x] 4.4 新增 [AUDIT] task 前与 Review Log 去重；审查发现的新问题作为 [AUDIT] task 追加，循环直到无新问题或 3 轮；**停下的那一刻交付 Review Log 报告**。**（Round-5 去重后新增 2 个 [AUDIT-R5] 项，见下；Round-6 确认无新问题后停轮。）**

## Review Log

| Round | Date | Agents | Issues Found | Issues Fixed | Remaining |
|-------|------|--------|-------------|-------------|-----------|
| 0（apply 前，对抗审查） | 2026-08-22 | 5 维度 × 40 agents（wfjkfmkp4，5.4M tokens） | 根因 **unanimously CONFIRMED**（teardown-order device UAF）；INIT-FAILURE-DOUBLE-DESTROY(HIGH→2.3)；imageView 异常修正为 `(x|(x<<40))` 编码合法句柄；waitIdle 前置冗余；3 假 doc URL；1.3/3.1-3.4 未跑 | 2.1 修正（不移动 waitIdle）、2.2→2.3、2.3→2.4、3.2 加 validation-log 验收、3.4 加全内存 dump、措辞/URL 纠正 | apply：2.1-2.4 实现；3.1-3.4 回归；imageView 留回归确认 |
| 1（apply 前，方案终审） | 2026-08-22 | 5 维度 × 30 agents（wdmwgmm3p，1.7M tokens） | `go_with_conditions`：2.2 幂等 guard 正确且必备（REL-1/TC-3），"改 m_Initialized"被反驳；主修复不搬迁（TC-2/REL-3）；REL-2（catch 漏 m_GPUFrameManager.Release）、G2（早期异常 null-deref）；AC-2/3/4 措辞；TC-6（m_QueueContext/m_ShaderImporter 无释放，非阻塞） | 2.2 补 gated m_GPUFrameManager.Release+guard null pApp（成 2.4）、2.3 移 imageView（成 2.5）、D2/Impact 措辞更正 | apply：2.1-2.4；1.3；3.1-3.4；观察 TC-6 |
| 2（apply 中，实测） | 2026-08-22 | 手工 stash-repro + read_dump/cdb | **本 change 核心假设被证伪**：起初误用 `TestSimpleTriangle` 得"崩溃消失"（错）。改用 `TestIMGUI` 实测：**基线 exit 1 / device=0xDD / ~IMGUIContext**（crash_20260822_173633，链完整确认）；**修复版仍 exit 1**（crash_20260822_173846，BREAKPOINT/abort：ucrtbased abort+terminate）；validation log teardown/0xDD=0。修复未消除崩溃。**疑似根因：Release 内 vk::waitIdle/destroy 可抛、放入 noexcept 析构→terminate。** 另 TestIMGUI draw-bind VUID 04007/07312 属另一条线 | 工件已改：崩溃真实未修复 + 疑似析构抛异常；proposal/design/tasks 按新理解重写 | **本 change 重定位**：不再修崩溃（归 fix-reverse-module-teardown-order 逆向 teardown），只做 vulkan 后端 teardown 健壮性+泄漏；析构异常安全（try/catch）为必需项 |
| 3（重定位后，对抗验证） | 2026-08-22 | 26 agents（wf_a3100715-2fc，2.6M tokens）+ D3D12 对照实测 | **钉死真实根因链**：①修复版 DLL 确含修复（obj/DLL 时间戳 17:44 > 源码 17:43，DLL 含 "Release() threw"）——**非 stale-DLL**；②`Release()` 停在 `m_GPUFrameManager.WaitIdle()`(:483，=vkDeviceWaitIdle，throwing) 抛 `vk::SystemError`，被析构吞后跳过窗口循环 → 窗口未标记 released → 0xDD；③**模块正序**才是不移之因：D3D12 正序却 exit 0（COM refcount + `WindowContext` 从不 deref backend + `Release()` 从不 `destroy()` device）→ 顺序是前提、Vulkan 无引用计数+裸 `pApp` 才是触发；④正确根 = 逆向 teardown（device 最后销毁），本 change dtor→Release 仅在其下安全。**Refute**：guard 提前早退 / stale-DLL / 只标窗口 / 只不毁 device（不完整，0xDD 会挪到字体/缓冲） | 本 change 重定位为"vulkan 后端 teardown 健壮性 + 泄漏"；新增 2.2（析构异常安全）主力项；崩溃归属 fix-reverse-module-teardown-order；回归标记为以逆向 teardown 为前提 | apply：2.2-2.4；逆向 teardown change 落地后 3.2-3.4 回归 + 4.x 审查闭环；观察 TC-6 |
| 4（重定位后，本轮审查） | 2026-08-23 | 5 维度 × 28 agents（wf_6f802924-dae，2.3M tokens） | **重定位正确**（三轴验证：本 change 不修崩溃且在逆向 teardown 下才安全；崩溃归正序/逆向 teardown 是对根；健壮性+泄漏独立必要）。**确认真实缺陷**：①HIGH `Release():483 WaitIdle` unguarded pApp-null deref + **body 全链 (:536/:539/:567/:570/:573/:576) 多为 unguarded，修法不能只 guard WaitIdle**（会 relocate）；②HIGH task 2.2 未实现 + **:581 第二个 `m_Device.waitIdle()` 未包**→device-lost 截断清理；③MED `m_Released=true` 前置→**try/catch 必须逐步**（外层单包会半清理）；④MED pre-device 未守护 Init 区(:216/:172-214/:256-261)抛→m_Released 非 false、pApp deref 崩；⑤MED 默认构造+Init 未调用同 risk；⑥MED 另一 tester `Main.cpp:427` 显式 `Release()` 无保护；⑦LOW-MED **措辞过度："各 destroy* 可抛"错**（void destroy 是 noexcept，只有 waitIdle 抛）；⑧LOW m_QueueContext 泄漏 + `:265-267` m_DebugMessenger 未置空。**Refute**：destroyImageView 可抛（no，void no-throw）；TC-6 判定 m_QueueContext/m_ShaderImporter 无需 release 的部分被反驳为"m_QueueContext 需补、ShaderImporter 无需"。**给逆向 change 反馈**：backend(index2)依赖更高 index 的 ShaderCompilerSlang(3)/WindowSystem(4)，"后来者依赖先来者"不变式不严格成立 | design D2 勘误（只 waitIdle 抛、逐步包）、D2 覆盖 :581、新增 D6（Release() body 全链 GetApp() guard）、D3 补 pre-device 缺口、2.2 勘误、新增 2.6 [AUDIT]、Open Questions 定论 destroyImageView | apply：2.2（逐步）+2.3（含 pre-device 修法）+2.4+2.6；逆向 teardown 落地后 3.2-3.4 回归 + 4.x 审查闭环；把"backend 依赖更高 index"隐患同步 fix-reverse-module-teardown-order |
| 5（最终签收，6b0d50a） | 2026-08-24 | 20 agents（wf_233925f2-58d，1.1M tokens，0 error，MCP 抓取核验） | **主维度全 CLEAN**：init-fail cleanup（stepped catch 释放全部可创建子对象）、module-teardown-path（reverse 循环正确且 bound-safe）、destructor-exception（析构 try/catch 防 terminate + per-step 正确 + m_Released 防双毁）、api-lifecycle（线程顺序合法）、code-tasks-honesty（2.3/2.4 代码完整）。**确认（非缺陷级）**：MED×2 = tasks 2.3/2.4 代码已完整但 tasks.md 未勾（记账）→ 已补；LOW = ①doc-comment :489/:610-611 低估 `vkDeviceWaitIdle` 抛域（实为 device-lost/out-of-device/host-memory/unknown/validation-failed 5 类；代码 catch 基类 vk::SystemError 已覆盖）→ 已修措辞；②第二个 waitIdle 冗余（verify 判定非缺陷，无害保留）；③WindowHandle 多态基类无虚析构（verify 判定不可达 + baseline 已有 + 接口层，本 change 未引入 → 记录为独立加固，不在本 change 范围）。**Refute**：destroyDebugUtilsMessengerEXT(null 句柄)（VK_NULL_HANDLE 为合法 destroy 参）、getPipelineCacheData 抛跳过 destroyPipelineCache（vkDestroyDevice 释放子对象）、Reverse-order-invariant 不严格（文档精度，无功能影响）、Reverse-loop robustness（不可达）、RecreateSwapchain m_Released=false（deliberate 防御，design 已记录）。**doc URL 复查**：vkDeviceWaitIdle/vkDestroyDevice 两 refpages 均存在+含 API+语义一致。**回归**：Round-0~4 八项在 6b0d50a 无回归 | doc 措辞 2 处修正；2.3/2.4/2.5/3.2/3.3/3.4 勾选 + 补实测说明；实施前待办 #98 完成 | apply：全部 2.x-4.x 闭环；2 个已知 LOW（冗余 waitIdle、WindowHandle 虚析构）记录为签收非阻塞项 |
| 6（修复确认，停轮） | 2026-08-24 | 3 agents（wf_6f9957b2-6ea，163k tokens） | 确认 R5 修复正确：doc-comment 措辞准确（5 失败码 + 基类 vk::SystemError 覆盖）、delta spec `vulkan-backend-teardown` 诚实不越权（未为 VMA 单资源泄漏/模块序所主张）、无功能回归、scope 边界正确。**微 LOW（2 项 spec 措辞精度，非缺陷）**：Req1-Scenario2 因果归属微不精（waitIdle 抛由 per-step catch 捕获、析构 catch 为兜底；净保证成立）→ 已按此处事实修精化；Req4 用 index 2/7 隐含"1 模块=1 factory"（次序性既约与 factory 数无关，load-bearing 成立）→ 已措辞为"注册序早/晚"回避 index 假定。**边界说明（非缺陷，无需修）**：逆序模块销毁在物理上随 commit 6b0d50a 与本 change 同 commit 落地，但语义归属已归档的 fix-reverse-module-teardown-order；本 change 工件 scope 为 RenderBackend_Vulkan teardown 自身，不涉及 CaModuleManager/VulkanBuffer/VulkanTexture 生命周期——此为客户验证的边界/记账 nuance，非回归 | spec 2 处措辞精度修正；Review Log 补 R6 行 | 停轮（无待修问题，终止条件"无新问题"达成）；归档 |

## Round-5 [AUDIT] 结果（去重后）

| AUDIT | 来源 | 决策 | 状态 |
|-------|------|------|------|
| [AUDIT-R5-1] doc-comment :489/:610-611 低估 `vkDeviceWaitIdle` 抛域 | Round-5 LOW | 修正措辞为完整 5 类失败码（文档精度，代码已正确 catch 基类） | ✅ 已修（RenderBackend_Vulkan.cpp 注释 2 处） |
| [AUDIT-R5-2] tasks 2.3/2.4 代码完整但未勾选（记账误差） | Round-5 MED×2 | 勾选 2.3/2.4 + 补实测说明 | ✅ 已修（tasks.md） |
| [AUDIT-R5-3] 第二个 `m_Device.waitIdle()` 冗余 | Round-5 LOW（verify 判定非缺陷：两段 waitIdle 均在 device destroy 前、均 per-step catch、无正确性/泄漏缺陷） | 记录为已知无害冗余，保留（移除需改行为、无收益且引入风险） | 签名（非阻塞） |
| [AUDIT-R5-4] `WindowHandle` 多态基类无虚析构 | Round-5 LOW（verify 判定不可达：全码无 `delete`/`unique_ptr<WindowHandle>` 经基类；baseline 已有、接口层、本 change 未引入） | 记录为独立加固建议，不作为本 change 任务（避免超 scope 改动共享接口） | 签名（非阻塞，建议单独 hardening） |
| [AUDIT-R5-5] vulkan TestIMGUI 仍未全绿 | Round-5 实测精化 | 归因 draw-bind VUID（04007/08114/07312，design Non-Goals 已声明独立）+ VMA 资源泄漏（`vk_mem_alloc.h:10492`，属 `fix-teardown-resource-lifecycle` 域） | 外部阻塞（另案），非本 change  |

